# Model Artifacts

Training and evaluation outputs are written here during local development, for example:

- `fake_news_model.joblib`
- `training_splits.joblib`
- `model_metrics.json`
- generated plot images

These files are ignored in Git because they are generated artifacts, not source code.
